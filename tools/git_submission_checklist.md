# Git ??????

## Git ????

```bash
git status --short
git diff --cached --stat
git remote -v
```

?????

- ??????????????????????
- ????????????????? mask ???????
- ?? `origin` ?? `https://github.com/Youyuan287/OVS-T.git`?

## Commit ? Tag

```bash
git add README_OVS_T.md .gitignore docs experiments tools scripts stage1 stage1_geometry_finetune eval data pyproject.toml README*.md sam3
git diff --cached --stat
git commit -m "submission: YYYYMMDD S1 short description"
git tag -a submit-YYYYMMDD-S1 -m "score=pending; change=<one variable>; package=<remote-or-local-path>; keep=pending"
```

?????????? `experiments/submission_log.md`??????????????? commit?

## ?????

??????`*.pt`?`*.pth`?`*.ckpt`?`*.zip`?`*.tar.gz`??????? mask???????`outputs/`?`checkpoints/`???????
